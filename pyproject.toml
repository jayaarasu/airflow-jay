[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "data-pipeline-lib"
version = "1.0.0"
description = "Reusable Airflow utilities"
authors = [{name="Jay"}]

[tool.setuptools]
packages = ["data_pipeline_lib"]